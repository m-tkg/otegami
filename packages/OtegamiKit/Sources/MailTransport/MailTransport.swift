/// Placeholder for the IMAP/SMTP transport protocol layer (`IMAPSessionProtocol`,
/// `SMTPSessionProtocol`) that `MailTransportMailCore` will implement and
/// `SyncEngine` will consume. Filled in starting M1.
public enum MailTransport {}
